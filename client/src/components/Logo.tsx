import { Typography } from "@mui/material";

function Logo() {
  return (
    <Typography variant="h2" component="h1" gutterBottom align="center">
      livepaint
    </Typography>
  );
}

export default Logo;
